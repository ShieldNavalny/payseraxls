# Paysera XLS Analyzer — зависимости

# — Ядро —————————————————————————————————————————————————————————————————————
pandas>=2.0
openpyxl>=3.1

# — PDF: извлечение текста ————————————————————————————————————————————————————
pdfplumber>=0.10       # текстовые PDF
pymupdf>=1.23          # подсветка в PDF + рендер страниц для OCR

# — OCR (для сканированных PDF) ———————————————————————————————————————————————
# Требует установки Tesseract на системе:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-lit
#   macOS:   brew install tesseract
pytesseract>=0.3.10
Pillow>=10.0

# — Fuzzy-матчинг вендоров ————————————————————————————————————————————————————
rapidfuzz>=3.0
